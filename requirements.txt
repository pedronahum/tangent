# Core runtime dependencies (mirrors the [project] dependencies in
# pyproject.toml) plus what is needed to run the test suite. Optional backends
# are installed via extras — note this fork is not on PyPI (PyPI's `tangent` is
# Google's unmaintained 2017 release), so extras must reference the repo:
#   pip install "tangent[tf] @ git+https://github.com/pedronahum/tangent.git"
# Available extras: tf, jax, torch, keras, tinygrad, symbolic (SymPy-based
# algebraic simplification), viz (matplotlib + networkx), test, all.
gast>=0.6.0,<0.8.0
numpy
# Test-suite only: autograd is the reference oracle for the gradient tests and
# is not required to use tangent itself (see the [test] extra).
autograd>=1.2
pytest>=6.0
# Test-suite only: drives the property-based gradient tests
# (tests/test_property_based.py) against the finite-difference oracle.
hypothesis>=6.0
