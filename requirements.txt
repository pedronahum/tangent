# Runtime dependencies (mirrors setup.py install_requires) plus pytest for
# running the test suite. Optional backends are installed via extras:
#   pip install tangent[tf]       TensorFlow support
#   pip install tangent[jax]      JAX support
#   pip install tangent[symbolic] SymPy-based algebraic simplification
#   pip install tangent[viz]      matplotlib + networkx visualization
#   pip install tangent[all]      everything above, including pytest
autograd>=1.2
future
gast>=0.6.0,<0.8.0
pytest>=6.0
numpy
