[metadata]
description-file = README.md

[tool:pytest]
filterwarnings =
    # astor is unmaintained (last release 0.8.1) and references deprecated
    # ast aliases (ast.Num, ast.Str, ...) at import time. This is upstream
    # noise that cannot be fixed from here.
    ignore:ast\..* is deprecated:DeprecationWarning
